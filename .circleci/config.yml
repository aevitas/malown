version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8
    steps:
      - run:
        name: install
        command: npm install
      - run:
        name: build
        command: npm run build